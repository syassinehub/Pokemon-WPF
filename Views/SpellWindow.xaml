<Window x:Class="PokemonWpf.Views.SpellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Spell List" Height="450" Width="600"
        Background="#F5F5F5">
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Spell List"
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="10" />

        <DataGrid x:Name="SpellDataGrid" AutoGenerateColumns="False"
                  Margin="10,50,10,10" Grid.Row="1" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Damage" Binding="{Binding Damage}" Width="100"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="View Monsters" Grid.Row="2" HorizontalAlignment="Right" Margin="10"
                Width="120" Height="30" Click="OnViewMonstersClick"/>
	</Grid>
</Window>
